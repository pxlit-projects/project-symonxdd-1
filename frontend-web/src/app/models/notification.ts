export interface Notification {
  postId: number;
  message: string;
  createdAt: string; // ISO date string
  // id: number;
  // postId: number;
  // message: string;
  // status: 'approved' | 'rejected';
  // createdAt: Date;
}
